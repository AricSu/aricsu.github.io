---
title: Serverless API
description: "`tihc-serverless` API, engines, and environment variables"
---

## Endpoint

`POST /api/stream_chat`

Request JSON:

```json
{ "messages": [{ "role": "user", "content": "..." }], "chat_engine": "openai", "stream": true }
```

Response:

- streaming text (`text/plain; charset=utf-8`)

## Engines

- `openai`
- `openai_rag` (GraphRAG → OpenAI)
- `manus` (proxy)
- `tidb` (proxy)

## Env Vars

See `tihc-serverless/.env.example`
